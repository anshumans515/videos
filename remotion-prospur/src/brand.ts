// Prospur brand tokens (mirror of PLAYBOOK.md §1). Shared by every
// composition so colours and the reel format live in one place.
export const BRAND = "#047857"; // primary green
export const POP = "#34D399"; // brighter mint — used over footage
export const SPEND = "#F59E0B"; // amber — the "spender"/expense side
export const SPEND_HOT = "#F87171"; // bad-outcome / reality-check accents
export const TEXT_DARK = "#16211C";
export const PAPER = "#F3EDE1";
export const GROUND = "#050B09"; // near-black studio ground used on reels

// Instagram Reels canvas — the default for every composition here.
export const REEL = { width: 1080, height: 1920, fps: 30 } as const;

export const ARN = "ARN-348873";
